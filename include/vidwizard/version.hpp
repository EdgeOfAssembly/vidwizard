/**
 * @file version.hpp
 * @brief vidwizard package version (CLI -v / --version).
 */
#pragma once

/** @brief Semantic version string; bump on meaningful behavior change. */
#define VIDWIZARD_VERSION "0.4.0-alpha"

/** @brief Program name used in usage and version lines. */
#define VIDWIZARD_NAME "vidwizard"
